import clasA, HelloHi, { Hello ,user} from './type';// 특정 한 타읍을 가져다 와서 사용할 수 있다.


const helloMessage:Hello = {
  text:'hello world!'
};

user.name

// new classA().a();

const u :HelloHi = {
  hi(){

  },
  text:''
}